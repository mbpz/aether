{
  "name": "aether",
  "version": "0.1.0",
  "description": "Multi-provider local-first agent framework — run AI agents on any LLM (Claude, Gemini, Bedrock, Ollama, DeepSeek) with a V8-Isolate sandbox and three-tier skill disclosure (≥60% token reduction).",
  "license": "Apache-2.0",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "engines": {
    "node": ">=20.0.0"
  },
  "scripts": {
    "dev": "npm run dev --workspaces --if-present",
    "build": "npm run build --workspaces --if-present",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage",
    "check:wasmtime": "node scripts/check-wasmtime.mjs",
    "check:ebpf": "node scripts/check-ebpf-agent.mjs",
    "gateway": "npm run dev -w packages/gateway",
    "sandbox": "npm run dev -w packages/sandbox",
    "ui": "npm run dev -w packages/ui",
    "aether": "node packages/gateway/bin/aether-audit.mjs"
  },
  "keywords": [
    "ai-agents",
    "agent-framework",
    "multi-provider",
    "local-first",
    "sandbox",
    "V8-Isolate",
    "zero-trust",
    "privacy",
    "skill-loader",
    "progressive-disclosure"
  ],
  "repository": {
    "type": "git",
    "url": "git+https://github.com/aether/aether.git"
  },
  "bugs": {
    "url": "https://github.com/aether/aether/issues",
    "email": "security@aether.local"
  },
  "homepage": "https://github.com/aether/aether#readme",
  "devDependencies": {
    "@vitest/coverage-v8": "^4.1.5",
    "esbuild": "^0.28.1",
    "tsx": "^4.22.4",
    "vitest": "^4.1.5"
  }
}
